version: '3.7'

services:

  sample:
    container_name: sample
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '${APP_SURREALIST_APP_DIR}:/app'
    ports:
      - 3001:3000
    environment:
      - CHOKIDAR_USEPOLLING=true